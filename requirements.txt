# ---------------------------------------------------------------------------
# Face-Verify-Chain — Python dependencies
# ---------------------------------------------------------------------------

# ── Face detection & 128-d encoding ────────────────────────────────────────
# We use DeepFace with the MTCNN backend for highly accurate face detection 
# and 128-d Facenet embeddings, avoiding the Apple Silicon compilation 
# issues common with dlib.
deepface==0.0.92
numpy==1.26.4
tf-keras==2.17.0
opencv-python-headless==4.10.0.84

# ── Image handling ──────────────────────────────────────────────────────────
Pillow==10.4.0

# ── HTTP (SerpAPI + imgbb uploads) ─────────────────────────────────────────
requests==2.32.3

# ── .env loading ───────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Blockchain / EVM ───────────────────────────────────────────────────────
# web3.py — Ethereum/EVM interactions (Ganache local or Polygon Mumbai)
web3==6.20.1

# ── Solidity compiler (only needed to deploy the contract) ─────────────────
# py-solc-x downloads solc binaries on-demand; not needed at runtime
py-solc-x==2.0.3

# ── UI/UX for Demo ─────────────────────────────────────────────────────────
rich==13.7.1

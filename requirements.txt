# ---------------------------------------------------------------------------
# Face-Verify-Chain — Python dependencies
# ---------------------------------------------------------------------------

# Face detection & 128-d encoding
# NOTE: face_recognition requires dlib, which must be compiled from source on
# Apple Silicon (M1/M2). See README for installation instructions.
face_recognition>=1.3.0
dlib>=19.24.0
numpy>=1.24.0

# Image handling
Pillow>=10.0.0

# HTTP clients (SerpAPI + imgbb uploads)
requests>=2.31.0

# Environment variable loading from .env
python-dotenv>=1.0.0

# ---------------------------------------------------------------------------
# Optional: upgrade these if you encounter compatibility issues
# ---------------------------------------------------------------------------
# cmake>=3.27          # needed to build dlib from source
# setuptools>=68.0.0

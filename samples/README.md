"""
samples/README.md — Sample Images
----------------------------------
Place your test images in this directory.

Quick start: run the helper to download a public-domain portrait:

    python samples/generate_sample.py

This saves samples/sample_face.jpg, which is the default image used by the
pipeline when you run:

    python main.py samples/sample_face.jpg --dry-run   # face detection only
    python main.py samples/sample_face.jpg             # full pipeline

You can also supply your own image:

    python main.py path/to/your_photo.jpg
"""
